# クラウド公開の手順（URLで社内共有する）

このツールをネット上に置いて、**相手にはURLを配るだけ**にする手順です。
相手はリンクを開くだけ（Python導入も設定も不要）で使えるようになります。

使うサービス（どちらも無料）:
- **GitHub** … コードを置く場所
- **Streamlit Community Cloud** … コードを読んでWebアプリとして公開する場所

所要時間：初回のみ 15〜20分ほど。一度置けば、あとはURLを配るだけです。

> ⚠️ 重要：**APIキーは GitHub に置きません。** この配布フォルダにキー（.env）は
> 入れていません。キーは最後に Streamlit 側の「Secrets」にだけ貼り付けます。

---

## ステップ1：GitHub にコードを置く

1. https://github.com にアクセス（アカウントが無ければ「Sign up」で無料作成）
2. 右上の「＋」→「New repository」
3. リポジトリ名を入力（例：`sales-target-selector`）
4. **「Private」を選ぶ**（社内限定にするため）→「Create repository」
5. 作成後の画面で「**uploading an existing file**」リンクをクリック
6. **この配布フォルダの中身をすべて**、ブラウザにドラッグ＆ドロップ
   （`app.py` や `src` フォルダ、`data` フォルダなど。**.env は入っていないのでそのままでOK**）
7. 下の「Commit changes」ボタンで確定

---

## ステップ2：Streamlit Cloud で公開する

1. https://share.streamlit.io にアクセス
2. 「Continue with GitHub」で、ステップ1と同じGitHubアカウントでサインイン
3. 「Create app」→「Deploy a public app from GitHub」を選ぶ
4. 次を指定：
   - **Repository**：先ほど作った `sales-target-selector`
   - **Branch**：`main`
   - **Main file path**：`app.py`
5. 「**Advanced settings**」を開き、**Secrets** 欄に次の1行を貼り付ける
   （`=` の右側を、あなたの実際のキーに置き換え。**引用符 " " は残す**）

   ```
   GOOGLE_PLACES_API_KEY = "AIzaSy...あなたのキー..."
   ```

6. 「Deploy」を押す → 数分待つと、`https://xxxx.streamlit.app` のURLが発行されます
7. **このURLを社内の人に配る**だけ。相手はリンクを開けば使えます。

---

## ステップ3：安全設定（必ず）

APIキーは Streamlit のサーバー側にあるので相手には見えませんが、
**URLを知っていれば誰でも使えて、その分あなたに課金**されます。次の2つを必ず：

1. **アプリを限定公開にする**
   - Streamlit Cloud のアプリ設定（Settings → Sharing）で
     「**Who can view this app**」を、閲覧を許可するメールアドレス（社内メンバー）に限定する。
2. **Google 側で使いすぎを防ぐ**
   - [認証情報](https://console.cloud.google.com/apis/credentials) でキーを
     「**Places API (New) のみ**」に制限。
   - [予算とアラート](https://console.cloud.google.com/billing/budgets) で月の上限を設定。

---

## 困ったとき

- **デプロイ後に「APIキーが設定されていません」と出る**
  → ステップ2-5 の Secrets を設定し忘れ／打ち間違い。Secrets を直して再デプロイ。
- **コードを直したい**
  → GitHub のファイルを更新すれば、Streamlit Cloud が自動で反映します。
- **URLを知らない人に使われたくない**
  → ステップ3-1 の限定公開を必ず設定。
